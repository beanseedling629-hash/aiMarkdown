# AI Markdown 插件 - 综合格式测试文档

> **测试说明**：本文档用于全面测试 AI Markdown 插件的渲染能力，包含各种 Markdown 语法和格式组合。

---

## 目录

1. [标题测试](#标题测试)
2. [段落与文本格式](#段落与文本格式)
3. [列表测试](#列表测试)
4. [代码块测试](#代码块测试)
5. [表格测试](#表格测试)
6. [链接与图片](#链接与图片)
7. [引用测试](#引用测试)
8. [分隔线与空行](#分隔线与空行)
9. [HTML 混排](#html-混排)
10. [脚注与缩写](#脚注与缩写)

---

## 标题测试

# 一级标题 H1

## 二级标题 H2

### 三级标题 H3

#### 四级标题 H4

##### 五级标题 H5

###### 六级标题 H6

=== 分隔线测试 ===

---

## 段落与文本格式

### 基础段落

这是一个普通段落，用于测试基本的文本渲染能力。Markdown 中段落之间需要有一个空行。

这是第二个段落，包含了多种文本格式：

- **粗体文本**（使用 `**` 或 `__`）
- *斜体文本*（使用 `*` 或 `_`）
- ***粗斜体文本***（组合使用）
- ~~删除线文本~~（使用 `~~`）
- `行内代码`（使用反引号）
- ==高亮文本==（部分解析器支持）
- <u>下划线文本</u>（HTML 标签）

### 转义字符测试

以下字符需要使用反斜杠转义：

\* 星号
\_ 下划线
\# 井号
\[ 方括号
\] 方括号
\( 圆括号
\) 圆括号
\! 感叹号
\< 尖括号
\> 尖括号

### 特殊符号

&copy; 版权符号 (©)
&reg; 注册商标 (®)
&amp; 和号 (&)
&lt; 小于号 (<)
&gt; 大于号 (>)
&mdash; 长破折号 (—)
&ndash; 短破折号 (–)

---

## 列表测试

### 无序列表

* 星号标记的列表项
* 第二个列表项
  * 嵌套列表项（2个空格缩进）
  * 另一个嵌套项
    * 三级嵌套
      * 四级嵌套
* 返回一级列表

- 减号标记的列表项
- 第二个项
- 第三个项

+ 加号标记的列表项
+ 第二个项

### 有序列表

1. 第一项
2. 第二项
3. 第三项
   1. 嵌套的有序列表
   2. 第二项
4. 返回主列表
5. 第五项
6. 第六项

### 任务列表

- [x] ✅ 已完成的任务
- [ ] ⏳ 未完成的任务
- [x] ✅ 另一个已完成任务
- [ ] 📝 待办事项
- [ ] 🔄 进行中的任务

### 定义列表

术语一
: 定义一的描述

术语二
: 定义二的描述
: 同一个术语的第二个定义

Markdown
: 一种轻量级标记语言

### 混合列表

1. 有序列表项一
   * 无序子项
   * 另一个无序子项
2. 有序列表项二
   - [ ] 任务子项
   - [x] 已完成子项
3. 有序列表项三

---

## 代码块测试

### 行内代码

在段落中使用行内代码：`const greeting = "Hello, World!";`

多个行内代码：`function` `const` `let` `var` `class` `interface`

带引号的代码：`` `backtick` 中使用反引号 ``

### 围栏代码块（Fenced Code Blocks）

#### JavaScript 示例

```javascript
// 快速排序实现
function quickSort(arr) {
    if (arr.length <= 1) return arr;
    
    const pivot = arr[Math.floor(arr.length / 2)];
    const left = arr.filter(x => x < pivot);
    const middle = arr.filter(x => x === pivot);
    const right = arr.filter(x => x > pivot);
    
    return [...quickSort(left), ...middle, ...quickSort(right)];
}

// 测试
console.log(quickSort([3, 6, 8, 10, 1, 2, 1]));
// 输出: [1, 1, 2, 3, 6, 8, 10]
```

#### Python 示例

```python
# 斐波那契数列生成器
def fibonacci(n):
    """生成斐波那契数列前 n 项"""
    a, b = 0, 1
    result = []
    for _ in range(n):
        result.append(a)
        a, b = b, a + b
    return result

# 打印前10个斐波那契数
for i, num in enumerate(fibonacci(10)):
    print(f"F({i}) = {num}")
```

#### TypeScript 示例

```typescript
interface User {
  id: number;
  name: string;
  email: string;
  isActive: boolean;
}

class UserService {
  private users: User[] = [];

  addUser(user: Omit<User, 'id'>): User {
    const newUser: User = {
      id: this.users.length + 1,
      ...user
    };
    this.users.push(newUser);
    return newUser;
  }

  getUserById(id: number): User | undefined {
    return this.users.find(user => user.id === id);
  }
}
```

#### HTML 示例

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>测试页面</title>
    <style>
        body { font-family: Arial, sans-serif; }
        .container { max-width: 1200px; margin: 0 auto; }
    </style>
</head>
<body>
    <div class="container">
        <h1>标题</h1>
        <p>段落内容</p>
    </div>
</body>
</html>
```

#### CSS 示例

```css
/* 现代 CSS 变量示例 */
:root {
    --primary-color: #3498db;
    --secondary-color: #2ecc71;
    --text-color: #333;
    --spacing-unit: 8px;
}

.container {
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    padding: calc(var(--spacing-unit) * 2);
}

.card {
    padding: 2rem;
    border-radius: 8px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    transition: transform 0.3s ease;
}

.card:hover {
    transform: translateY(-4px);
}
```

#### SQL 示例

```sql
-- 创建用户表
CREATE TABLE users (
    id INT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) NOT NULL UNIQUE,
    email VARCHAR(100) NOT NULL UNIQUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 查询活跃用户
SELECT u.username, u.email, COUNT(p.id) as post_count
FROM users u
LEFT JOIN posts p ON u.id = p.user_id
WHERE u.is_active = TRUE
GROUP BY u.id
HAVING post_count > 10
ORDER BY post_count DESC;
```

### 缩进代码块（传统格式）

    // 这是用四个空格缩进的代码块
    function test() {
        const message = "traditional code block";
        return message;
    }

    // 可以包含多行
    // 保留缩进格式

### 无语言标识的代码块

```
这是一个没有指定语言的代码块
可以用于显示任意文本内容
保留原有的空格和格式
支持多行内容
```

---

## 表格测试

### 基础表格

| 姓名 | 年龄 | 城市 | 职业 |
|------|------|------|------|
| 张三 | 28 | 北京 | 工程师 |
| 李四 | 32 | 上海 | 设计师 |
| 王五 | 25 | 深圳 | 产品经理 |
| 赵六 | 30 | 广州 | 数据分析师 |

### 对齐方式测试

| 左对齐 | 居中对齐 | 右对齐 |
|:-------|:--------:|-------:|
| 左 | 中 | 右 |
| 文本 | 文本 | 文本 |
| ABC | DEF | GHI |
| 100 | 200 | 300 |

### 复杂表格（带格式化）

| 功能 | 描述 | 状态 | 优先级 | 完成度 |
|------|------|------|--------|--------|
| Markdown 解析 | 支持标准 Markdown 语法 | ✅ 完成 | 高 | 100% |
| 语法高亮 | 代码块语法高亮显示 | ✅ 完成 | 高 | 100% |
| 表格支持 | 渲染 Markdown 表格 | 🔄 进行中 | 中 | 75% |
| 导出 PDF | 导出为 PDF 格式 | ⏳ 待开发 | 低 | 0% |
| 协作编辑 | 多人实时协作 | ⏳ 待开发 | 高 | 0% |
| 版本历史 | 查看编辑历史 | ✅ 完成 | 中 | 90% |

### 包含格式化的表格

| 元素 | 示例 | 说明 | 快捷键 |
|------|------|------|--------|
| 粗体 | **重要** | 强调文本 | `Ctrl+B` |
| 斜体 | *关注* | 轻微强调 | `Ctrl+I` |
| 代码 | `print()` | 行内代码 | `` Ctrl+` `` |
| 链接 | [点击](https://example.com) | 超链接 | `Ctrl+K` |
| 标题 | `# 标题` | 标题格式 | `Ctrl+H` |

### 长文本表格

| 项目 | 详细描述 | 技术栈 | 备注 |
|------|----------|--------|------|
| AI Markdown | 一个强大的 Markdown 编辑器和查看器插件，支持实时预览、语法高亮、主题切换等功能 | TypeScript, React, Vite | 核心产品 |
| 测试文档 | 用于验证插件功能的正确性和完整性，包含各种 Markdown 语法的边界情况 | Markdown, Node.js | 测试用途 |

---

## 链接与图片

### 行内链接

这是一个 [百度链接](https://www.baidu.com)，这是一个 [GitHub 链接](https://github.com)。

带标题的链接：[CodeBuddy](https://www.codebuddy.ai "CodeBuddy 官网")

### 参考式链接

[Google][1]
[GitHub][2]
[MDN Web Docs][3]

[1]: https://www.google.com "Google 搜索"
[2]: https://github.com "GitHub"
[3]: https://developer.mozilla.org "MDN Web 文档"

使用后引用：

我已参考了 [Google] 和 [GitHub] 以及 [MDN Web Docs] 的文档。

### 图片

#### 行内图片

![示例图片](https://via.placeholder.com/150 "占位图片 150x150")

![GitHub Logo](https://github.githubassets.com/images/modules/logos_page/GitHub-Mark.png "GitHub Logo")

#### 参考式图片

![Logo][logo]
![Banner][banner]

[logo]: https://via.placeholder.com/100x50 "Logo 图片 100x50"
[banner]: https://via.placeholder.com/600x200 "Banner 图片 600x200"

### 链接与图片混合

这是一个包含图片的链接：

[![点击跳转](https://via.placeholder.com/120x60)](https://example.com)

### 邮箱和 URL 自动链接

<https://example.com>
<https://github.com>
<user@example.com>
<test.email@domain.co.jp>

### 相对路径链接

[返回首页](../README.md)
[查看图片](./images/photo.png)
[访问文档](./docs/index.html)

### 锚点链接

[跳转到标题测试](#标题测试)
[跳转到表格测试](#表格测试)

---

## 引用测试

### 基础引用

> 这是一级引用
> 
> 引用可以包含多个段落。

> 引用中的第二段落。

### 嵌套引用

> 这是外层引用
>
> > 这是嵌套的内层引用
> >
> > 嵌套可以继续
> >
> >> 第三层嵌套
> >
> > 返回第二层
>
> 返回外层引用

### 引用中的其他元素

> ### 引用中的标题
> 
> 1. 引用中的有序列表
> 2. 第二项
> 
> * 引用中的无序列表
> * 第二项
> 
> `代码` 在引用中也可以使用。
> 
> | 表格 | 在引用中 |
> |------|---------|
> | 单元格 | 内容 |
> 
> > 多层嵌套引用测试
> > 
> > **粗体**和*斜体*也能在引用中使用

### 长引用

> Markdown 是一种轻量级标记语言，它允许人们使用易读易写的纯文本格式编写文档，然后转换成有效的 HTML 文档。
> 
> 由约翰·格鲁伯（John Gruber）创建于 2004 年，使用亚伦·斯沃茨（Aaron Swartz）开发的格式化规则。
> 
> 本测试文档用于验证 AI Markdown 插件对引用格式的渲染是否正确，包括嵌套引用、引用中的列表、代码块等各种元素。

### 引用与代码块

> 以下是一个代码示例：
> 
> ```python
> def hello():
>     print("Hello, World!")
> ```
> 
> 代码块在引用中应该正确缩进和渲染。

---

## 分隔线与空行

### 分隔线样式

以下几种写法都可以产生分隔线：

---

***

*****

- - -

_ _ _

### 分隔线使用场景

#### 章节分隔

上述内容是一个章节。

---

上述内容是另一个章节，使用分隔线区分。

#### 内容分组

第一组内容

---

第二组内容

---

第三组内容

### 空行测试

## 标题后无空行
这是紧接标题的文本，测试是否需要空行。

## 标题后有空行

这是分开的文本，有空行分隔。

### 多个空行

这是一个段落。



多个空行在渲染时应该被合并成一个空行。


这里有一个空行。

### 列表后的空行

* 列表项 1
* 列表项 2

下一个段落应该与列表有适当的间距。

1. 有序列表项 1
2. 有序列表项 2

下一个段落。

### 代码块后的空行

```
代码块
```

下一个段落。

### 引用后的空行

> 引用内容

下一个段落。

---

## HTML 混排

### HTML 标签在 Markdown 中

#### 换行与排版

使用 HTML 的 `<br>` 标签强制换行<br>这里换了新行。

<p>这是一个 HTML 段落，可以有多个句子。在这个段落中我们可以添加<span style="color: red;">彩色文本</span>。</p>

<div style="background: #f0f0f0; padding: 10px; border-radius: 5px;">
这是 div 容器中的内容，可以有<span style="font-weight: bold;">粗体</span>和<span style="font-style: italic;">斜体</span>。
</div>

#### 文本格式化

<strong>HTML 粗体（strong）</strong>

<em>HTML 斜体（em）</em>

<del>HTML 删除线（del）</del>

<mark>HTML 高亮（mark）</mark>

<small>小号文本（small）</small>

<sup>上标（sup）</sup>

<sub>下标（sub）</sub>

#### 列表

<ul>
<li>HTML 无序列表项 1</li>
<li>HTML 无序列表项 2
    <ul>
    <li>嵌套项</li>
    </ul>
</li>
<li>HTML 无序列表项 3</li>
</ul>

<ol>
<li>HTML 有序列表项 1</li>
<li>HTML 有序列表项 2</li>
<li>HTML 有序列表项 3</li>
</ol>

#### 表格

<table>
<thead>
<tr>
<th>HTML 表格</th>
<th>第二列</th>
<th>第三列</th>
</tr>
</thead>
<tbody>
<tr>
<td>单元格 1</td>
<td>单元格 2</td>
<td>单元格 3</td>
</tr>
<tr>
<td>单元格 4</td>
<td>单元格 5</td>
<td>单元格 6</td>
</tr>
</tbody>
</table>

#### 图片与链接

<img src="https://via.placeholder.com/100" alt="HTML 图片" width="100" height="100">

<a href="https://example.com" target="_blank">HTML 链接（新窗口打开）</a>

### Markdown 与 HTML 混合

这是 **Markdown 粗体**，这是 <em>HTML 斜体</em>。

> Markdown 引用中
> <span style="color: red;">HTML 内容</span>
> 可以继续 **Markdown 格式**

### 注释

<!-- 这是一个 HTML 注释，在渲染后的页面中不可见 -->

[//]: # (这是 Markdown 注释)

### 详情折叠

<details>
<summary>点击展开详情</summary>

这是折叠的内容，可以包含：

* 列表项
* 列表项

1. 有序列表
2. 第二项

```python
# 甚至代码块
def test():
    pass
```

</details>

### 视频嵌入

<video controls width="250">
    <source src="/media/cc0-videos/flower.webm" type="video/webm">
    <a href="/media/cc0-videos/flower.webm">Download video</a>
</video>

---

## 脚注与缩写

### 脚注

这是一个带脚注的句子[^1]。

这是另一个脚注示例[^note]。

可以在文中多次引用同一个脚注[^1]。

脚注可以包含**格式化文本**和`代码`[^formatted]。

### 定义脚注

[^1]: 这是第一个脚注的内容，可以包含多个句子。脚注通常用于提供引用来源或补充说明。

[^note]: 这是命名脚注的内容，可以很长，
    甚至可以跨越多行。

    可以有多个段落。
    
    第二个段落的内容。

[^formatted]: 脚注中可以包含**粗体**、*斜体*、`代码`等格式。

### 缩写

HTML 是超文本标记语言。

CSS 是层叠样式表。

API 是应用程序编程接口。

JSON (JavaScript Object Notation) 是一种轻量级的数据交换格式。

*[HTML]: Hyper Text Markup Language
*[CSS]: Cascading Style Sheets
*[API]: Application Programming Interface
*[JSON]: JavaScript Object Notation

---

## 数学公式（如果支持）

行内公式：当 $a \ne 0$ 时，方程 $ax^2 + bx + c = 0$ 的根为：

块级公式：

$$
x = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a}
$$

---

## 总结

本文档涵盖了 Markdown 的大部分常用语法，包括：

- ✅ 标题（1-6级）
- ✅ 段落和文本格式
- ✅ 列表（有序、无序、任务、定义）
- ✅ 代码块（多种语言）
- ✅ 表格（各种对齐）
- ✅ 链接和图片
- ✅ 引用（嵌套）
- ✅ 分隔线
- ✅ HTML 混排
- ✅ 脚注和缩写

使用本文档可以全面测试 AI Markdown 插件的渲染能力和兼容性。

---

**文档版本**: v1.0
**最后更新**: 2026-05-09
**作者**: AI Markdown 测试团队
